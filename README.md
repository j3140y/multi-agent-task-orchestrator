
# Multi-Agent MVP

A minimal multi-agent system with Planner / Executor / Tool / Reviewer.

## Setup
```bash
pip install -r requirements.txt
```

Create `.env`:
```
OPENAI_API_KEY=your_key
```

## Run
```bash
python main.py
```
